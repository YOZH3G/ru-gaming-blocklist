name: Update Fallout 76 AWS ranges

on:
  pull_request:
    paths:
      - "games/Fallout76_AWS.txt"
      - "scripts/update_fallout76_aws.py"
      - ".github/workflows/update-fallout76-aws.yml"
  schedule:
    - cron: "37 4 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Refresh Fallout 76 AWS ranges
        run: python scripts/update_fallout76_aws.py

      - name: Validate production lists
        run: python scripts/validate_blocklists.py

      - name: Verify committed snapshot on pull request
        if: github.event_name == 'pull_request'
        run: git diff --exit-code -- games/Fallout76_AWS.txt

      - name: Commit scheduled changes
        if: github.event_name != 'pull_request'
        run: |
          if git diff --quiet -- games/Fallout76_AWS.txt; then
            echo "Fallout 76 AWS ranges are already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add games/Fallout76_AWS.txt
          git commit -m "Update Fallout 76 AWS ranges"
          git push
